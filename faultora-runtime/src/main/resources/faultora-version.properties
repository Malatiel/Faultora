# Filtered by Maven at build time. The version a plugin's compatibility range
# is compared against has to be the version this actually is — a constant in
# source would be a second place to remember on every release, and the release
# after somebody forgot would refuse plugins for a version that never shipped.
version=${project.version}
